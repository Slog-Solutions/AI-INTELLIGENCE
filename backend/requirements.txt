# ATIP Backend Dependencies
# =========================================================
# Core API
fastapi==0.118.0
uvicorn[standard]==0.30.6
python-multipart==0.0.20

# Auth
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
PyJWT==2.10.1

# Database
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10

# Document parsing
python-docx==1.1.2
pymupdf>=1.26.0

# Data processing
pandas==2.2.3
openpyxl==3.1.5

# Vector store
chromadb>=1.0.0

# ─────────────────────────────────────────────────────────
# Enterprise RAG Pipeline
# ─────────────────────────────────────────────────────────

# Embeddings — BAAI/bge-large-en-v1.5 (or bge-m3, set via EMBEDDING_MODEL env var)
sentence-transformers>=2.7.0
transformers>=4.40.0
torch>=2.1.0

# BM25 keyword retrieval (hybrid search)
rank-bm25>=0.2.2

# Reranking — FlashRank (ms-marco-MultiBERT-L-12 or MiniLM-L-12)
flashrank>=0.2.10

# Numerical
numpy>=1.26.0

# ─────────────────────────────────────────────────────────
# Utilities
# ─────────────────────────────────────────────────────────
python-dotenv==1.1.1
email-validator==2.2.0
requests==2.31.0
httpx==0.27.2

# Testing
pytest==8.3.4